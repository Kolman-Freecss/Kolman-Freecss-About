import { ScrollManagerDirective } from './scroll-manager.directive';

describe('ScrollManagerDirective', () => {
  it('should create an instance', () => {
    const directive = new ScrollManagerDirective();
    expect(directive).toBeTruthy();
  });
});
